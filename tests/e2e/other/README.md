# Other E2E specs (not in scope for auth-journey branch)

These specs are kept for reference or future use. The default Playwright config runs only `auth-journey.spec.ts`.

To run all E2E tests including these:

```bash
npx playwright test tests/e2e/
```

To run only the auth journey:

```bash
npm run test:e2e
```
